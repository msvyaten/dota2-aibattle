# Archived Match Log Pointer

The hand-maintained historical log is in git history:
`git show ae2604d:docs/match_log.md`.
Current match evidence is generated from console logs with `tools/postmatch.py` and
`tools/match_stats.py`.
